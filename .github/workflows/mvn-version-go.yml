name: GO Integration Test - Show maven version 

on: [push]

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
    - name: environment
      run: printenv
    - name: maven-version
      uses: actions/setup-go@v1
      with:
        go-version: '1.13.x'
      run: mvn -version
