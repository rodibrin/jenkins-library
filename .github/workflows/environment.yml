name: GO Integration Test - Show maven version 

on: [push]

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
    - name: setup
      uses: actions/setup-go@v1
    - name: environment
      run: printenv
    - name: maven-version
      run: mvn -version
